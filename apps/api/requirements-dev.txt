# Rocky API — dev/test dependencies. Install with runtime:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1             # ASGITransport for hermetic API tests
