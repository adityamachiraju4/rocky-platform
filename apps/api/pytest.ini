[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = strict
addopts = -m "not parity"
markers =
    parity: PostgreSQL migration/metadata parity guard; requires TEST_DATABASE_URL with CREATE DATABASE privileges. Run with: pytest -m parity
